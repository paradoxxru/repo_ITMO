“очка входа - index.php
1) аутентификаци€: получить текущего пользовател€ со всеми правами доступа
require_once ("../app/user.php");
$user = new User();


2) роутинг: определить, какую страницу запрашивает пользователь и есть ли у него соответствующие права.

ƒл€ каждой типовой странницы будет свой класс.
	- CCartController.php  класс, отвечающий за формировани€ страницы с корзиной
	- CCatalogController.php  класс, отвечающий за формирование страницы каталога
	- CProductController.php  класс, отвечающий за формирование страницы конкретного товара

Ќазвание запрашиваемого раздела "прилетит" в GET параметре.(этот параметр будет прописан в ссылках)
		//index.php?q=catalog
		//или q=cart
		//или q=product
require_once ("../app/router/CRouter.php");
$controller = CRouter::getController(); - получаем объкт, одного из классов выше.
	¬ классе CRouter прописан метод getController, который по GET-параметру( q ) определ€ет какого класса будет $controller.
	“о есть определ€ет какой раздел запрашивают. ≈сли параметр q не задан, то дефолтным считаетс€ раздел catalog.

3) сформировать запрашиваемую страницу и отправить в браузер пользователю
 3.1) ≈сли запрашивают catalog (то есть $controller = new CCatalogController() )
	
	$controller->setPermissions($user->getAllPermissions()); - устанавливаем разрешени€ пользовател€ исход€ из аутентификации
	
	$controller->render(); - вызываем метод формировани€ раздела на странице.
	    ¬ этом методе:
		a) из конфигурационного файла(базы данных) получаем все товары в виде массива
			$parser = CConfParser::getInstance('../app/config/db_product.yaml');
			$parser->read();
			$goods = $parser->getAllParams();
		b) определ€ем/задаем шаблон html страницы, которую нужно отразить(catalog.php - шаблон страницы каталога, 
		cart.php - шаблон страницы корзины, product.php - шаблон страницы конкретного продукта, в папке views\product 
		шаблоны отображени€ продукта на различных страницах)
			$path_to_template = "../app/views/catalog.php"; - «јƒјЋ» путь к шаблону руками
			include($path_to_template); - подключили соответствующий шаблон
